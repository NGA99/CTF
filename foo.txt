# DiceCTF 2022 WriteUp
## knock-knock
#### environment
NodeJS
#### Service structure
...
#### Goal
When the service is started, the flag is stored in a note with an id value of 0 by createNote function.
	if we find out tokens with an ID value of 0, We can read flag.

#### Vulnerabilities and Bug
A secret key is used to generate tokens.
The scret key has \`secret-${crypto.randomUUID}\`.

